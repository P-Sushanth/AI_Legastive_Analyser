# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# OS
.DS_Store
Thumbs.db

# Project Specific
dataset/*.pdf
!dataset/.gitkeep
# (Optional) If you want to keep the PDFs on GitHub, remove the line above. 
# But for typical repos, large binaries are better handled elsewhere or kept if small.
# User's request implies they want these files too though.

# Logs
*.log
